function sayHello() {
  //DEFINING OR DECLARING FUNCTION
  console.log("Hello");
}

sayHello(); //CALLING FUNCTION

function add(num1, num2) {
  console.log(num1 + num2);
}

add(5, 10);

function subtract(num1, num2) {
  return num1 - num2;
}
// we return so that we can do or share that value, and when we console.log only then it comes up in console
subtract(10, 2);
